Place your trained weights here.

- Expected path inside the container: `/app/weights/best_model.pth`
- The baseline training script saves checkpoints like `best.pt` / `last.pt`; rename/copy the one you want to submit to `best_model.pth`.

